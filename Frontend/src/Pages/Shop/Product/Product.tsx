import './Product.scss'

type productProps = {
    demographic: string,
    category: string,
}

export default function Product({demographic, category}: productProps) {
    
    return (
        <main className='productMainContent'>

        </main>
    )
}